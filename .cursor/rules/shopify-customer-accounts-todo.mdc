---
description: Reminder — finish Shopify customer account setup for site login
alwaysApply: true
---

# Pending: Shopify customer accounts

When the user works on login, account, or checkout, **remind them** this still needs Admin setup if auth fails or is untested:

1. Storefront API app scopes: `unauthenticated_write_customers` + `unauthenticated_read_customers` (reinstall/refresh token if scopes were added after install).
2. **Settings → Customer accounts** — enable classic/password (“Legacy”) customer accounts so Storefront `customerCreate` / `customerAccessTokenCreate` work.
3. Restart Next.js after env/token changes.

Login UI lives at `/account`; session cookie + checkout `buyerIdentity` are already wired in code.
