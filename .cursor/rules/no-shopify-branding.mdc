---
description: Domain live checklist for thefinepixel.com; no Shopify branding
alwaysApply: true
---

# Domain + branding

Canonical site: **https://www.thefinepixel.com** (`NEXT_PUBLIC_SITE_URL`). Apex `thefinepixel.com` should redirect to `www` in DNS/Vercel.

## Audit follow-ups (code)

Done in app:
- No local demo catalog when Shopify mode is on
- Coupon Remove clears cart discounts
- Related products from real catalog
- Review submissions paused (email CTA); POST returns 503
- `error.tsx` / `global-error.tsx`
- Default OG image; newsletter → email CTA (no fake subscribe)

Still on you (Admin / deploy): theme redirect, WELCOME30.
